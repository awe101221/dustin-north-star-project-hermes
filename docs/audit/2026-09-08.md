# Daily Hermes audit — 2026-09-08

First run of the recurring engineering audit. Playbook: [`docs/audit/PLAYBOOK.md`](PLAYBOOK.md).
Public repo: do not put NAV, keys, or tokens in this file.

## Snapshot

| Item | Value |
|---|---|
| Date (UTC) | 2026-09-08 |
| `origin/main` | `97fc816` `[verified] add 10 + 10 company models and mobile redesign` |
| Production | Vercel project `dustin-north-star-hermes`, deploy `dpl_BXMCshXoVgoU8YYSEAuKMwoN8YEM`, READY, same SHA |
| App | https://dustin-north-star-hermes.vercel.app |
| Brain | INVESTING-BRAIN-AG `cwiaqczpifnxxcucqwvr` |
| Runtime errors (7d) | none clustered |
| HTTP mix (24h, that deploy) | 213×200, 13×401, 5×307, 1×304 |
| GitHub issues before this run | none |
| Linear | no Hermes project; DUS-* is the older Awe Capital factory |

Product today: **10 + 10** home (`/`), Learnings, Research, Pipeline, North Star, Companies, Quant, Personas, Knowledge, Agents, Activity, Settings, Portfolio as context. README and smoke still described the original 16-route portfolio hub.

This run did **not** execute a live-data Playwright smoke against production (README ISSUES #3). Do not treat the UI as browser-verified against live rows.

## Freshness (live SQL, counts and dates only)

Performance series names are `portfolio` and `benchmark` (QQQ total-return index). There is **no** `qqq` series.

| Metric | Rows | Latest |
|---|---|---|
| `hermes_performance_points` portfolio TWR | 177 | 2026-09-04 |
| `hermes_performance_points` benchmark (QQQ) | 417 | 2026-08-31 |
| `hermes_trades` | 781 | 2026-08-28 |
| `hermes_ideas` | 76 | 2026-09-07 |
| `hermes_notes` | 6 | 2026-09-07 |
| `ibkr_positions` (all history) | 8101 | 2026-08-24 |
| `hermes_positions_latest` | 376 | 2026-08-24 |
| `ibkr_nav_history` | 33 | 2026-08-24 |
| `mission_benchmark` | 33 | 2026-08-24 |
| `analyst_memos` | 2406 | 2026-09-02 |
| `hermes_research_stream` | 1844 | 2026-09-07 |
| `hermes_persona_catalog` | 3 | 2026-06-10 |

Pipeline mix: live 25, monitor 24, sourcing 20, diligence 7. Agent task queue: empty.

Position classification (README ISSUES #6, re-checked): **156 / 376** latest positions have a sector; **220** do not.

Personas: `brad-gerstner`, `mohnish-pabrai`, `public-vc` (all active).

## P0 — trust the numbers

1. **Book is 15 days behind the mandate clock.** Latest IBKR position/NAV/`mission_benchmark` observation is 2026-08-24. Portfolio TWR stops 2026-09-04. QQQ benchmark series stops 2026-08-31. North Star alpha, hub YTD, and exposure charts cannot be fresher than the slowest of these.
2. **Live schema was ahead of this repo.** `hermes_schema_migrations` listed a 4th file `20260907000400_underwriting_graph_evaluation.sql` applied 2026-09-08 01:19 UTC, with no matching file under `supabase/migrations/`. Live objects: `hermes_prompt_versions`, `hermes_agent_runs`, `hermes_underwriting_nodes/edges`, `hermes_forecasts` / `_outcomes`, view `hermes_forecast_evaluations`, plus SECURITY DEFINER `hermes_replace_underwriting_graph` and `hermes_grade_forecast_outcome`. **This PR reconstructs that migration from live DDL** so `npm run db:migrate` can reproduce production. Remaining unfiled live tables: `hermes_sleeves*` (app already reads them from `/portfolio`).
3. **10 + 10 models are frozen in git.** `COMPANY_MODEL_AS_OF = 2026-09-07T20:55:00.000Z` in `src/lib/company-models.ts`. Ranked IRRs can look precise while quotes and memos move. Best-ideas scoring is a completeness heuristic unless a snapshot was published.

## P1 — product and security hygiene

1. README still called `/` the Portfolio Hub and listed 16 routes. Smoke missed `/learnings` and `/portfolio`.
2. `src/lib/env.ts` no longer refused a non-`cwiaqczpifnxxcucqwvr` URL (AGENTS.md still claimed it did). Script clients in `scripts/lib/rest.ts` still checked. **This PR restores the app guard.**
3. 26 tables have RLS disabled (legacy backups/staging, `retired_securities`, `ranking_channels`, `analyst_memo_refreshes`, `hermes_schema_migrations`). Same class as README ISSUES #4. Do **not** auto-enable.
4. Vercel Authentication is on for `all_except_custom_domains`. App password gate (`HERMES_ACCESS_PASSWORD`) is separate. HMAC falls back to `password::fallback-secret` if `HERMES_SESSION_SECRET` is unset (`src/lib/auth.ts`).
5. Repo is public. Keep audit files free of NAV and secrets.

## P2 — keep getting more useful

1. Tests: stats, backtest, nav, learnings, best-ideas, company-models. No API/route/db query tests. CI is lint + typecheck + vitest + build.
2. Memos last analyzed 2026-09-02; agent queue empty. Underwriting loop is idle.
3. `QQQ_PRIOR_YEAR_CLOSE` is still a 2026 constant in `src/lib/db/hub.ts` (README ISSUES #7).
4. Hardcoded 10 + 10 ticker list in `CURRENT_10_PLUS_10_TICKERS` can drift from the live snapshot / idea table.

## Recommendations

| Priority | Action | Why |
|---|---|---|
| P0 | Daily IBKR position + NAV + `mission_benchmark` ingest through  the latest session | Hub and classification are on 2026-08-24 |
| P0 | Extend `migrate:performance` (or a sibling job) so `benchmark` and `portfolio` series do not lag the book | Alpha vs QQQ is only as fresh as both legs |
| P0 | Move company models off git seeds into `hermes_*` (forecasts table already exists live) | Rankings should not require a deploy to change |
| P1 | File `hermes_sleeves*` into a dated migration the same way underwriting was reconstructed | Portfolio page already depends on them |
| P1 | Require `HERMES_SESSION_SECRET` whenever the password gate is on; drop the password-derived HMAC fallback | Public repo + guessable cookie |
| P1 | Enable RLS **with SELECT policies** on leftover `hermes_schema_migrations` only after deciding whether anon should see hashes | Advisor noise vs leak of migration names |
| P2 | Smoke + a thin `/api/health` check in CI against a preview URL when secrets exist | README ISSUES #3 is still the hole |
| P2 | Wire best-ideas score to model IRRs / `hermes_forecasts` when graded | Heuristic scores are not the mandate |
| P2 | Decision: Linear Hermes project vs this rolling GitHub issue as the tracker | DUS-* is a different factory |

## What this audit learned (change tomorrow)

- QQQ lives in `hermes_performance_points.series = 'benchmark'`. A query for `'qqq'` returns zero and looks like a data outage.
- `list_tables` row counts on this project are not trustworthy; use SQL `count(*)`.
- A migration can land on the live brain from another agent without a file in this repo. Always diff `hermes_schema_migrations` against `supabase/migrations/`.
- Sleeves, prompt versions, and the underwriting graph already exist live; the TypeScript app only partially knows about them (sleeves yes, graph no).
- Engineering audits stay in `docs/audit/`. Investing learnings stay in `/learnings`.

## Out of scope / do not do

- Enable RLS on the 26 flagged tables from this audit.
- Write the reference DB (`vnxypnpepwxurhbdtswn`).
- Touch `kms-brain`.
- Drop or rewrite legacy tables.
- Treat stooq backtests as attribution (README ISSUES #8).
